# ---- UI Framework ----
streamlit              # Simple web app frontend to upload resumes and display results

# ---- Google Gemini API ----
google-generativeai     # Official Gemini Pro SDK for text and vision models (Pro Vision)

# ---- Environment Variables ----
python-dotenv           # Load your API keys (like Google API) securely from .env file

# ---- PDF to Image Conversion (no OS deps) ----
PyMuPDF                # Render PDF pages to images without poppler
Pillow                 # Image handling for previews